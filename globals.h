//////////////////////////////////////////////////////////////////////////////////////////
// externals 
//////////////////////////////////////////////////////////////////////////////////////////
extern void winReshapeFunc(GLint w, GLint h);
extern void renderScene();
extern void update();

//////////////////////////////////////////////////////////////////////////////////////////
// constants 
//////////////////////////////////////////////////////////////////////////////////////////
const int SCREEN_WIDTH	= 500;
const int SCREEN_HEIGHT = 500;

const int PASSES		= 4;

typedef unsigned int	u32	;

// amount of blobs
static float gridSize = 60.0f;
static bool halfTone = true;
static bool fsaa = false;
static bool applyObjectColour = false;
static bool rotation = true;
static bool lamp = true;
static bool torus = false;
static bool fly = false;

//////////////////////////////////////////////////////////////////////////////////////////
// structures 
//////////////////////////////////////////////////////////////////////////////////////////
struct sRGB
{
	float r, g, b;
};

// Vector 3 struct ( x , y, z)
struct vec3
{
	GLfloat x;
	GLfloat y;
	GLfloat z;
};

// Vector 2 Struct (x, y)
struct vec2
{
	GLfloat x;
	GLfloat y;
};